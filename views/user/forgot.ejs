<%- include('../layouts/header') -%>

 <!--------------------------- section of body --------------------------->
 <section class="bg-clr text-white d-flex align-items-center">
    <% if(typeof bUser !== 'undefined'){ %>
        <div class="container-fluid d-flex justify-content-center position-absolute">      
            <div class="alert alert-warning text-danger alert-dismissible fade show" role="alert">
                <i class="fa-solid fa-triangle-exclamation"></i>  <strong><%= bUser.name %>, You are blocked!</strong> You may not use the site.
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        </div>
    <% } %> 
    <div class="container mt-3  d-flex justify-content-center">
        <div class="log-box-ful row bg-white text-black">
            <div class="dis-part1 col-md-4 pt-5 pb-2 ">
                <h2 class="fw-bold mb-3 ">Forgot Password</h2>
                <p>Get access to your Orders, Wishlist and Recommendations</p>
                <img class="crtn-img img-fluid mx-auto" src="img/cartoon.png">
            </div>
            <div class="col-md-8  my-auto text-center">
                <% if(typeof mes !== 'undefined'){ %>
                    <p class="text-primary"><%= mes %></p>
                <% } %> 
                <form class="mb-3" method="post">
                    <input class="form-inp" type="email" name="email" placeholder="Enter email" required>
                    <button type="submit" class="log-btn w-100 mt-3 text-black">Submit</button>
                </form>      
            </div>
        </div>
    </div>
</section>

<%- include('../layouts/footer') -%>